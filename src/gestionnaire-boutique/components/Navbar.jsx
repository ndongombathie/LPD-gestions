// Petit wrapper pour compatibilité : certaines pages importent `Navbar`.
// Nous réexportons ici le `Header` existant pour garder les imports inchangés.
import Header from './Header';

export default Header;
